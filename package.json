{
  "name": "superset-devin-demo",
  "version": "0.1.0",
  "description": "GitHub Action that spawns a Devin session when an issue is labeled 'devin'. Devin opens a PR against this repo.",
  "private": true,
  "type": "module",
  "scripts": {
    "upload-knowledge": "tsx src/upload-knowledge.ts",
    "devin:issues:clone": "tsx src/devin-issues-clone.ts",
    "devin:issues:ls": "tsx src/devin-issues-ls.ts",
    "devin:sessions:ls": "tsx src/devin-sessions-ls.ts",
    "typecheck": "tsc --noEmit",
    "docker:build": "docker build -t superset-devin-demo .",
    "docker:spawn": "docker run --rm --env-file .env -e ISSUE_NUMBER -e ISSUE_TITLE -e ISSUE_BODY -e ISSUE_URL -e TARGET_REPO -e UPSTREAM_REPO superset-devin-demo"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.0"
  }
}
